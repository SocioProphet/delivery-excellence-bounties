# Bounty policy v0

Deterministic payout function:
payout = base_rate(stream) * complexity_points * impact_mult * urgency_mult * risk_mult

Payout gating requires:
- PR merged
- DoD satisfied
- maintainer approval recorded
- evidence links present
